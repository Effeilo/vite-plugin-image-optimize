# Contributing

Contributions are welcome. Whether you want to report a bug, suggest an improvement, add a feature, or fix a typo - feel free to participate.

---

## Reporting an issue

Open an issue on the GitHub repository to:

- Report a bug or unexpected behavior.
- Suggest an improvement or new feature.
- Discuss an idea before submitting a pull request.

When reporting a bug, include:

- Your Node.js version (`node -v`)
- Your Vite version (`npm list vite`)
- Your operating system
- The error message and stack trace if applicable
- A minimal reproduction if possible

---

## Submitting a pull request

1. Fork the repository.
2. Create a dedicated branch:

```bash
git checkout -b my-proposal
```

3. Make your changes.
4. Run the test suite:

```bash
npm test
```

5. Commit with a clear message:

```bash
git commit -m "Fix: description of the change"
```

6. Push the branch:

```bash
git push origin my-proposal
```

7. Open a pull request on GitHub.

---

## Running locally

```bash
# Install dependencies
npm install

# Build the plugin
npm run build

# Watch mode during development
npm run watch

# Run tests
npm test

# Run tests in watch mode
npm run test:watch
```

---

## Best practices

- Stay true to the focused scope of the plugin: image generation and compression during `vite build`.
- Only change what is necessary. Focused changes are easier to review.
- Add or update tests for any behavior you change.
- Test your changes on macOS or Linux, and on Windows if the change touches file system operations.
- Consult the [changelog](../CHANGELOG.md) to understand the history of decisions.

---

## Project structure

```
├── dist/                  compiled output (generated by tsc)
├── src/
│   ├── index.ts           plugin entry point and Vite hooks
│   ├── types.ts           ImageOptimizeOptions interface
│   ├── utils.ts           getAllImages, fileIsNewer, formatSize
│   └── types/
│       └── shims.d.ts     module type declarations
├── tests/                 Vitest test suite
├── docs/                  this documentation
├── scripts/
│   └── fix-extensions.js  legacy script (no longer used)
├── tsconfig.json
├── vitest.config.ts
└── package.json
```

---

## Credits

`vite-plugin-image-optimize` is built on top of:

- [sharp](https://sharp.pixelplumbing.com/) by Lovell Fuller - high-performance image processing
- [svgo](https://svgo.dev/) - SVG optimization
- [globby](https://github.com/sindresorhus/globby) by Sindre Sorhus - file system glob scanning
